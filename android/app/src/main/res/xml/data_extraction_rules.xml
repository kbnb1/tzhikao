<?xml version="1.0" encoding="utf-8"?>
<!--
     Sample data extraction rules file; uncomment and modify as needed.

     See https://developer.android.com/training/data-storage/manage-backup-data
     for details about data extraction rules.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> and <exclude> to control what's backed up to the cloud. -->
        <exclude domain="sharedpref" path="device.xml"/>
    </cloud-backup>

    <device-transfer>
        <!-- TODO: Use <include> and <exclude> to control what's transferred via D2D. -->
        <exclude domain="sharedpref" path="device.xml"/>
    </device-transfer>
</data-extraction-rules>
